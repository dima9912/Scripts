for _, plot in pairs(workspace.Plots:GetChildren()) do
    if plot:IsA("Model") then
        local at = plot:GetAttribute("Owner")
        if at == game.Players.LocalPlayer.UserId then
--Use plot.sdsdsdsd

--Functions

local function anim2track(asset_id)
    local objs = game:GetObjects(asset_id)
    for i = 1, #objs do
        if objs[i]:IsA("Animation") then
            return objs[i].AnimationId
        end
    end
    return asset_id
end

animid = anim2track(89114994401113)

local function generatorrandomsymbols()
    local length = math.random(20,20)
    local array = {}
    for i = 1, length do
        array[i] = string.char(math.random(32, 126))
    end
    return table.concat(array)
end

local function protectnamebase() 
    plot.PlotSign.SurfaceGui.Frame.TextLabel.Text = generatorrandomsymbols()
end

--Locales
local LSSOS = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local StarterGui = game:GetService("StarterGui")
local args = {
    plot.Name
}
local animation = Instance.new("Animation")
animation.AnimationId = animid
anim = Players.LocalPlayer.Character:FindFirstChildWhichIsA("Humanoid"):LoadAnimation(animation)
anim.Priority = Enum.AnimationPriority.Movement
--Gui
local Folder = Instance.new("Folder")
Folder.Parent = CoreGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Parent = Folder

--frame gui
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 200, 0, 0)
frame.Position = UDim2.new(0, 1380, 0, 0)
frame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
frame.Draggable = true
frame.Active = true
frame.Selectable = true
frame.Parent = ScreenGui

local destroybutton = Instance.new("TextButton")
destroybutton.Size = UDim2.new(0, 37, 0, 37)
destroybutton.Position = UDim2.new(0, 165, 0, -5)
destroybutton.Rotation = -5
destroybutton.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
destroybutton.BackgroundTransparency = 1
destroybutton.Text = "X"
destroybutton.TextSize = 14
destroybutton.TextScaled = true
destroybutton.TextColor3 = Color3.fromRGB(255, 255, 255)
destroybutton.Font = Enum.Font.SourceSans
destroybutton.TextTransparency = 1
destroybutton.Parent = frame

local Uicornermathhyge = Instance.new("UICorner")
Uicornermathhyge.Parent = destroybutton
Uicornermathhyge.CornerRadius = UDim.new(0,8)

local sound1 = Instance.new("Sound")
sound1.Parent = frame
sound1.SoundId = "rbxassetid://90864973986637"

local sound2 = Instance.new("Sound")
sound2.Parent = frame
sound2.SoundId = "rbxassetid://9081625499"

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 30)
UICorner.Parent = frame

local UIStroke = Instance.new("UIStroke")
UIStroke.Parent = frame
UIStroke.Thickness = 2
UIStroke.Color = Color3.fromRGB(255, 255, 255)

local TextLabel = Instance.new("TextLabel")
TextLabel.Size = UDim2.new(1, 0, 1, 0)
TextLabel.Position = UDim2.new(0, -18, 0, -195) 
TextLabel.BackgroundTransparency = 1
TextLabel.Text = ""
TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TextLabel.TextSize = 23
TextLabel.Font = Enum.Font.Gotham
TextLabel.Parent = frame

local button1 = Instance.new("TextButton")
button1.Parent = frame
button1.Size = UDim2.new(0, 130, 0, 50)
button1.Position = UDim2.new(0, 38, 0, 30)
button1.Text = "Тп на базу"
button1.Font = Enum.Font.Gotham
button1.TextSize = 15
button1.TextTransparency = 1
button1.BackgroundTransparency = 1

local UICorner2 = Instance.new("UICorner")
UICorner2.Parent = button1


local button2 = Instance.new("TextButton")
button2.Parent = frame
button2.Size = UDim2.new(0, 130, 0, 50)
button2.Position = UDim2.new(0, 38, 0, 110)
button2.Text = "Авто-спиздилка"
button2.Font = Enum.Font.Gotham
button2.TextSize = 15
button2.TextTransparency = 1
button2.BackgroundTransparency = 1

local UICorner4 = Instance.new("UICorner")
UICorner4.Parent = button2


local button3 = Instance.new("TextButton")
button3.Parent = frame
button3.Size = UDim2.new(0, 130, 0, 50)
button3.Position = UDim2.new(0, 38, 0, 190)
button3.Text = "Быстрая активация"
button3.Font = Enum.Font.Gotham
button3.TextSize = 14
button3.TextTransparency = 1
button3.BackgroundTransparency = 1

local UICorner3 = Instance.new("UICorner")
UICorner3.Parent = button3

local button4 = Instance.new("TextButton")
button4.Parent = frame
button4.Size = UDim2.new(0, 130, 0, 50)
button4.Position = UDim2.new(0, 38, 0, 270)
button4.Text = "Защита ника"
button4.Font = Enum.Font.Gotham
button4.TextSize = 14
button4.TextTransparency = 1
button4.BackgroundTransparency = 1

local UICorner4 = Instance.new("UICorner")
UICorner4.Parent = button4

local button5 = Instance.new("TextButton")
button5.Parent = frame
button5.Size = UDim2.new(0, 130, 0, 50)
button5.Position = UDim2.new(0, 38, 0, 350)
button5.Text = "Танец эпштейна"
button5.Font = Enum.Font.Gotham
button5.TextSize = 14
button5.TextTransparency = 1
button5.BackgroundTransparency = 1

local UICorner5 = Instance.new("UICorner")
UICorner5.Parent = button5

button3.MouseButton1Click:Connect(function()
    task.spawn(function()
        local success, err = pcall(function()
            local workspace = game:GetService("Workspace")
            
            local function makeInstant(prompt)
                if not prompt or not prompt:IsA("ProximityPrompt") then return end
                
                pcall(function() prompt.HoldDuration = 0 end)
                pcall(function() prompt.RequiresLineOfSight = false end)
                
                pcall(function()
                    prompt:GetPropertyChangedSignal("HoldDuration"):Connect(function()
                        task.spawn(function()
                            task.wait(0.01)
                            pcall(function() prompt.HoldDuration = 0 end)
                        end)
                    end)
                end)
            end

            for _, obj in workspace:GetDescendants() do
                if obj:IsA("ProximityPrompt") then
                    task.spawn(makeInstant, obj)
                end
            end

            workspace.DescendantAdded:Connect(function(obj)
                if obj:IsA("ProximityPrompt") then
                    task.wait()
                    task.spawn(makeInstant, obj)
                end
            end)
        end)
    end)
end)

button1.MouseButton1Click:Connect(function()
Players.LocalPlayer.Character.HumanoidRootPart.CFrame = plot.AnimalTarget.CFrame
end)

destroybutton.MouseButton1Click:Connect(function()
Folder:Destroy()
end)

button2.MouseButton1Click:Connect(function()
    if autosteal == false then
    wait(0)
                                StarterGui:SetCore("SendNotification", {
                        Title = "Авто-спиздилка",
                        Text = "Включена",
                        Duration = 2,
                    })
        getgenv().autosteal = true
        while autosteal do
            if Players.LocalPlayer:GetAttribute("Stealing") == true then
                for i = 1, 20 do
                    game:GetService("ReplicatedStorage"):WaitForChild("Packages"):WaitForChild("Net"):WaitForChild("RE/dfdd4236-1fac-4b9a-9f67-fec68d72c151"):FireServer(unpack(args))
                end
            end
            task.wait(0.1)
        end
    else
    wait()
        getgenv().autosteal = false
                                    StarterGui:SetCore("SendNotification", {
                        Title = "Авто-спиздилка",
                        Text = "Отключена",
                        Duration = 2,
                    })
    end
end)

button4.MouseButton1Click:Connect(function()
plot.PlotSign.SurfaceGui.Frame.TextLabel:GetPropertyChangedSignal("Text"):Connect(protectnamebase)
protectnamebase()
StarterGui:SetCore("SendNotification", {
    Title = "Защита ника",
    Text = "Включено (Больше не надо актировать либо краш)",
    Duration = 8,
    })
sound2.Playing = true
task.wait(8)
sound2.Playing = false
end)

button5.MouseButton1Click:Connect(function()
    if emoteanimgetgenv == false then
        getgenv().emoteanimgetgenv = true
        StarterGui:SetCore("SendNotification", {
            Title = "Танец эпштейна",
            Text = "Включено",
            Duration = 4,
        })
        anim:Play()
    else
        getgenv().emoteanimgetgenv = false
		anim:Stop()     
        StarterGui:SetCore("SendNotification", {
            Title = "Танец эпштейна",
            Text = "Отключено",
            Duration = 4,
        })
    end
end)

--function
local text = "Меню УРБ💩🥀"
local function typewriterEffect(textToPrint)
    for i = 1, #textToPrint do
        TextLabel.Text = string.sub(textToPrint, 1, i)
        task.wait(0.03)
    end
end

--Anims
local frameinf = TweenInfo.new(1.5, Enum.EasingStyle.Cubic, Enum.EasingDirection.Out)
local frameinf2 = {Size = UDim2.new(0, 200, 0, 420)}
local frameanim = LSSOS:Create(frame, frameinf, frameinf2)
local buttonaimka = TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
local button1Anim = LSSOS:Create(button1, buttonaimka, {BackgroundTransparency = 0, TextTransparency = 0})
local button2Anim = LSSOS:Create(button2, buttonaimka, {BackgroundTransparency = 0, TextTransparency = 0})
local button3Anim = LSSOS:Create(button3, buttonaimka, {BackgroundTransparency = 0, TextTransparency = 0})
local button4Anim = LSSOS:Create(button4, buttonaimka, {BackgroundTransparency = 0, TextTransparency = 0})
local button5Anim = LSSOS:Create(button5, buttonaimka, {BackgroundTransparency = 0, TextTransparency = 0})
local textguiAnim = LSSOS:Create(TextLabel, buttonaimka, {TextTransparency = 0})
local destroyAnim = LSSOS:Create(destroybutton, buttonaimka, {BackgroundTransparency = 0, TextTransparency = 0})
frameanim:Play()
task.wait(1.5)

task.wait(0.2)
textguiAnim:Play()
destroyAnim:Play()
typewriterEffect(text)
task.wait(0.1)
button1.TextColor3 = Color3.fromRGB(0, 0, 0)
button1Anim:Play()
task.wait(0.1)
button2.TextColor3 = Color3.fromRGB(0, 0, 0)
button2Anim:Play()
getgenv().autosteal = true
getgenv().autosteal = false
task.wait(0.1)
button3.TextColor3 = Color3.fromRGB(0, 0, 0)
button3Anim:Play()
task.wait(0.1)
button4.TextColor3 = Color3.fromRGB(0, 0, 0)
button4Anim:Play()
task.wait(0.1)
button5.TextColor3 = Color3.fromRGB(0, 0, 0)
button5Anim:Play()
getgenv().emoteanimgetgenv = true
getgenv().emoteanimgetgenv = false
task.wait(0.2)
sound1:Play()

StarterGui:SetCore("SendNotification", {
    Title = "Меню УРБ",
    Text = "Гуи загружено",
    Duration = 5,
})

--доп хуйня
game.Players.LocalPlayer.DevEnableMouseLock = true
workspace:WaitForChild("TSP"):Destroy()
workspace.Sounds:WaitForChild("Day"):Destroy()
workspace.Sounds:WaitForChild("Night"):Destroy()

print("Название Базы: " .. plot.Name)
--Бля что такое эта
end
end
end
